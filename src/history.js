//custom made history object used in place of React Router's
//made so we can access history in our action creators
import { createBrowserHistory } from 'history';

export default createBrowserHistory();